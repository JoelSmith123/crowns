{
  "name": "crowns",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Crowns — a Bauhaus-styled LinkedIn Queens puzzle. Fully client-side, all puzzles randomized and uniquely solvable.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^26.0.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.9"
  }
}
